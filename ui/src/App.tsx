import React, { FC } from 'react';
import { AppContainer } from './components/layout/AppContainer';

const App: FC = () => (
    <div className="App">
        <AppContainer />
    </div>
);


export default App;
